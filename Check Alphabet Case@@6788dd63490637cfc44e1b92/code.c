#include <stdio.h>
void main()
{
    char ch;
    scanf("%c",&c);
    if(isupper(ch))
    {
        printf("Uppercase");
    }
    else if(islower(ch))
    {
        printf("Lowercase");
    }
    else
    {
        printf("Not an alphabet");
    }
}